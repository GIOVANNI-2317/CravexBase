#pragma once
#include <env/bridge.hpp>

namespace drawing {
    inline void init() {
        // registerBridgeMethod("Drawing.new", handleDrawingNew);
        // registerBridgeMethod("cleardrawcache", handleClearDrawCache);
    }
}