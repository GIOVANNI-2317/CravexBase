#pragma once
#include <env/bridge.hpp>

namespace utils {
    inline void init() {
        // registerBridgeMethod("identifyexecutor", handleIdentifyExecutor);
        // registerBridgeMethod("getexecutorname", handleGetExecutorName);
        // registerBridgeMethod("lz4compress", handleLz4Compress);
    }
}