#pragma once
#include <env/bridge.hpp>

namespace closure {
    inline void init() {
        // registerBridgeMethod("hookfunction", handleHookFunction);
        // registerBridgeMethod("newcclosure", handleNewcclosure);
        // registerBridgeMethod("checkcaller", handleCheckCaller);
    }
}
