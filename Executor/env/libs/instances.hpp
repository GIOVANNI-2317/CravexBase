#pragma once
#include <env/bridge.hpp>

namespace instances {
    inline void init() {
        // registerBridgeMethod("fireclickdetector", handleFireClickDetector);
        // registerBridgeMethod("getinstances", handleGetInstances);
        // registerBridgeMethod("getcustomasset", handleGetCustomAsset);
    }
}