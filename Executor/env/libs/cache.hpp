#pragma once
#include <env/bridge.hpp>

namespace cache {
    inline void init() {
        // registerBridgeMethod("cloneref", handleCloneref);
        // registerBridgeMethod("invalidate", handleInvalidate);
        // registerBridgeMethod("iscached", handleIscached);
    }
}
