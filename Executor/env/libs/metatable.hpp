#pragma once
#include <env/bridge.hpp>

namespace metatable {
    inline void init() {
        // registerBridgeMethod("setrawmetatable", handleSetRawMetatable);
        // registerBridgeMethod("getrawmetatable", handleGetRawMetatable);
        // registerBridgeMethod("setreadonly", handleSetReadOnly);
    }
}